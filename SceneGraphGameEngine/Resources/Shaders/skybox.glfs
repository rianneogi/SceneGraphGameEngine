#version 330

in vec3 TexCoord0;

out vec4 FragColor;

uniform samplerCube gCubemapTexture;
uniform vec3 gSkyColor;

void main()
{
    //todo - make this work with any texture
    //FragColor = texture(gCubemapTexture, TexCoord0);
    FragColor = vec4(gSkyColor,1);
}